# Moid-Share Backend

Node.js + Express + MongoDB (Mongoose) API and Socket.IO realtime layer.

**Not yet implemented.** This is Step 2 of the build plan (see
`../docs/ARCHITECTURE.md`). It will store **only user information** — users,
registered devices, settings — and never files or clipboard data.
